# 更新日志

> BISU 笔溯 · 网文创作桌面助手 — 安装包与更新元数据仓库（源码见 `kule-2025/bisu-src`）

## v0.3.12 (2026-07-27)

### Fixed（来自用户截图的 9 项问题）
- **排行榜页内导航冗余**：删除左侧 `<aside>` 内部导航栏，仅保留顶部快捷榜单 tab + 主内容区。
- **排行榜抓取失败静默回退**：改为明示具体失败原因（网络错误 / 反爬限制 / 25s 超时），`ranking.rs` 的 `RankBoard` 新增 `fetch_error` 字段并在 UI banner 展示。
- **技能中心重设计**：改为 100% 原创、对齐 WorkBuddy 视觉的卡片网格 + 分类 tab + 搜索布局，无残留旧代码。
- **左侧导航冗余合并**：删除重复的「账号」入口，原「管理中心」合并为「设置」，无重复路由。
- **欢迎页功能项收敛**：向导步骤与按钮精简，去除重复项。
- **工作台布局收敛**：不再有过挤 / 不可拖拽的 4 栏布局残留。
- **LLM「模型不存在」(HTTP 400 / code:1211)**：`ChatMessage` 支持 `content`/`reasoning` Option，非流式提取优先 `content` 回退 `reasoning`；`llm_chat` 增加 base_url/api_key 空值校验；404 分支给出模型名排查提示。
- **SenseNova 两模型（6.7-flash-lite JSON 解析失败 / u1-fast 404）**：通用机制已覆盖（`is_sensesnova` / `normalize_image_size` 尺寸处理、reasoning 回退、404 模型名提示）。
- **智能体对话页重设计**：重构为对齐 WorkBuddy 的左右分栏（左：会话列表/模式/助手/专家技能；右：消息区），消除孤儿 JSX 与未闭合括号。

### Security
- **启动错误日志根因已根治**（延续 0.3.11）：Rust 侧全量 `eprintln!` → `log_*` 宏 + 文件日志；前端 `console.error` 生产环境静默。
- **更新签名密钥轮换**：旧的 rsign 格式私钥与 Tauri signer 不兼容，已重新生成标准 Ed25519 密钥对，客户端验签公钥由 `79044C0B488CA03C` 轮换为 `9B6BFF206F56CB22`，自动更新通道验签恢复正常。

### Test
- 前端 `npm run build`（tsc + vite build）通过；`vitest` 186 passed；`cargo check` 0 error / 0 warning。

## v0.3.9 (2026-07-26)

- 源码保护部署 + 多项优化整合
- 无感更新机制对齐
- 修复 v0.3.8 → v0.3.9 应用内更新时的黑屏 / 闪屏问题

> 注：v0.3.10 / v0.3.11 为小幅维护版本。

## v0.3.8 (2026-07-26)

### 新增
- 无感自动更新机制：后台静默检查更新，发现新版本后通过非侵入式横幅提示用户，一键下载并自动重启
- 更新器双 endpoint 策略：Gitee 优先 + GitHub 兜底，国内用户下载更稳定
- 灰度发布通道：支持按设备比例灰度推送，确保更新稳定性

### 修复
- 全局竞品名称清理：彻底移除所有第三方产品名称引用，确保 100% 原创
- 更新器 endpoint 仓库名纠正：Gitee 仓库地址更新为正确值
- 侧边栏图标渲染 bug 修复：图标正确显示而非纯文本
- 各页面竞品引用清理：UpdateBanner、DeAIPanel、Ranking、RefineProPanel 等

### 其他
- 版本号统一更新至 0.3.8
